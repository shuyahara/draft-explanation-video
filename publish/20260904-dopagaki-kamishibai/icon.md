# チャンネルアイコン案（ずんだ人文学）

コンセプト: ずんだもんの公式立ち絵（坂本アヒル氏、改変可）の顔はそのまま使い、小物
（学者帽・眼鏡・髭）を重ねる。生成AIは不使用（キャラの顔は描き換えていない）。

> **2026-09-05 追記1**: v1（Pillow の図形描画で小物を手描き）はユーザー FB
> 「Pillow で描いた小物には納得していない。角帽・丸眼鏡・ひげはネットのフリー素材を
> 拾って付けるのがよい」を受けて **v2（フリー素材の透過 PNG を合成）** に作り直した。
>
> **2026-09-05 追記2**: さらにユーザーが自ら角帽＋丸眼鏡＋口髭のセット画像（1枚）を
> 持ち込み、それを切り抜いて使う **v3（ユーザー提供素材版）** を作成した。
> **現行の推奨案は v3**（`icon3-1` / `icon3-2`）。v1（`icon-1`〜`icon-3`）・v2
> （`icon2-1`〜`icon2-3`）は指示によりファイルを残しているが、参考用。

## v3: ユーザー提供素材版（現行）

生成の流れ: ①`prep_user_parts.py` でユーザー提供の1枚画像を3パーツ（角帽・丸眼鏡・口髭）に
切り抜き・透過化・ウォーターマーク除去する一回限りの前処理 → ②`make_icon3.py` で
v1/v2 と共通の顔ランドマーク・黒板背景・白縁付け・800×800キャンバス・円形プレビュー生成の
仕組み（`make_icon.py` / `make_icon2.py` を `import`）を再利用しつつ、3パーツを合成する。

```
C:\Users\shuya\Projects\script-to-video\.venv\Scripts\python.exe prep_user_parts.py
C:\Users\shuya\Projects\script-to-video\.venv\Scripts\python.exe make_icon3.py
```

### 出典

ユーザー提供（イラストボックスのサンプル画像と思われる。正式ダウンロード版とライセンス確認は
ユーザー側で実施予定）。元画像: `C:\Users\shuya\.claude\uploads\5e94f440-c8e0-463c-9354-f899b45feb01\b4a78672-image.jpg`
（作業コピー: `assets-kamishibai\icon-parts\user-set-original.jpg`、1480×1440 JPEG、白背景に
黒シルエットの角帽・丸眼鏡・口髭のセット）。透かし「ILLUST BOX」（グレー文字）が帽子ドーム中央に
入っていたため除去した（下記）。

### 出力

| ファイル | 内容 |
|---|---|
| `icon3-1.png` / `icon3-1-circle.png` | 角帽＋丸眼鏡＋口髭（表情 `smug.png`） |
| `icon3-2.png` / `icon3-2-circle.png` | 角帽＋丸眼鏡＋口髭（表情 `normal.png`） |

`icon3-N.png` は 800×800 RGB。`icon3-N-circle.png` は円形トリミングの検品用プレビュー
（RGBA）。背景（黒板）・白縁（`add_paper_outline`, 12px）・800×800 キャンバスへの配置ロジックは
v1/v2 と共通（`make_icon.py` の `board_background` / `add_paper_outline` / `T_SCALE` 等）。

### パーツ切り出し（`prep_user_parts.py`）

保存先: `C:\Users\shuya\Projects\assets-kamishibai\icon-parts\`（Git 管理外）。

1. **ウォーターマーク除去**: 帽子のバウンディングボックス `(0,0)-(1480,848)`（ボード＋ドーム。
   タッセルの垂れ下がり部分は含まない）内で、輝度 `40≤L≤222`（黒でも白でもない中間グレー）の
   画素を黒 `(0,0,0)` に塗りつぶす。ウォーターマーク文字「ILLUST BOX」の実測輝度は
   概ね L=60〜205 で、この範囲に収まっていた。眼鏡・口髭の領域はバウンディングボックス外
   なので触れない。
2. **透過化**: 輝度から滑らかなアルファを作る。`L≤120` で不透明（黒）、`L≥210` で透明、
   その間は線形グラデーション（JPEG のブロックノイズ・境界のギザギザ対策）。
3. **パーツ分割**: 実測した行・列の空白ギャップに基づく矩形＋マスクで3つに分ける
   （連結成分ラベリングは使わず、タッセルの列範囲 `x=1145-1310` を帽子側に加えるだけで
   単純な矩形の組み合わせで足りた）。
   - 角帽: `y<850`（ボード+ドーム全体）∪ `x=1145-1310`（タッセルの垂れ下がり部分）
   - 丸眼鏡: `y=850-1150, x=300-1160`
   - 口髭: `y=1150-1440, x=300-1160`（`y<1171` の左右端は眼鏡フレーム下端の破片が
     数十px混入するため、`x=650-850` の中央帯だけ残して除去した）
4. **口髭の内側を白で塗る**: 透過処理で内側（背景と同じ白）も透明になってしまうため、
   アーチ状の内側だけを対象に `ImageDraw.floodfill` で不透明の白 `(255,255,255,255)` に
   塗り直した。下段の歯（scallop）部分は元からベタ塗りの黒で塗りつぶし不要な上、画像の
   下端でちょうど切れており（アーチ部分と違って輪郭が閉じていない）、歯を含めて
   フラッドフィルすると背景全体に白が漏れる不具合が出た。実測で `y<1375` はアーチが
   2本足（閉じた輪郭）、`y≥1380` で歯が始まる（開いた輪郭）ことを確認し、フラッドフィルは
   `y=1150-1375` の範囲だけに限定して適用した（歯の部分は元のアルファのまま）。

### パーツの配置（`make_icon3.py`）

各パーツ画像内のアンカー座標を、v1/v2 と同じ顔ランドマークへ合わせて拡縮・配置した
（`make_icon2.py` の `place_part` を再利用）。

| パーツ | アンカー（パーツ内ローカル座標） | 意味 | 合わせ先（sprite座標） | 拡縮率 |
|---|---|---|---|---|
| 角帽 | (739, 848) | ドーム下端の中心（頭に接する点） | `(HAT_CX=350, 125)` | 0.17 |
| 丸眼鏡 | (410, 161) | 左右レンズ中心の中点 | 両目中心の中点 | `(EYE_R.x-EYE_L.x)/396 ≈ 0.492` |
| 口髭 | (305, 3) | アーチ頂点（上端中央） | `(FACE_CX=345, MOUTH.y-20=383)` | `210/611 ≈ 0.344` |

丸眼鏡のフレーム太さは実測 38px（元画像）で、拡縮後は sprite 空間で約19px・最終キャンバスで
約22px相当となり、「3px 未満」の下限を十分に上回ったためアルファ膨張は不要だった。

### つまずいた点と対処

- **角帽の頂点が最終キャンバスの上端で切れる不具合**: 角帽の「アンカー(ドーム下端)→頂点」の
  距離が v1/v2 の手描き帽子より長い意匠だったため、`TOP_MARGIN`（拡張キャンバスの余白）を
  160→230 に広げても直らなかった。原因を切り分けたところ、`TOP_MARGIN` が防ぐのは
  「拡張スプライトキャンバス内でのクリップ」であり、今回の不具合は別の段階
  ──800×800の最終キャンバスへ変換する際に、頂点が単純にキャンバスの外（y<0）に
  出てしまう──ことだと判明した。対処として、角帽の拡縮率を 340px幅相当(v1/v2と同じ)から
  0.17（板幅約251px相当）へ縮小し、かつ合わせ先の y 座標を 90→125 に下げて、頂点が
  キャンバス内に収まるようにした（`TOP_MARGIN` の 230 への拡張自体は無害な変更として残した）。
- **口髭のフラッドフィルが背景全体に漏れる不具合**: 最初、口髭パーツ全体（アーチ＋歯）を
  一括でフラッドフィルしたところ、白が背景全体に広がって透明領域が全て不透明白になって
  しまった。原因は歯の部分の輪郭が画像の下端で物理的に切れており（ソースJPEG自体が
  そこで終わっている）、輪郭が閉じていなかったため。対処として、行ごとの黒画素の
  連結成分数（run数）を調べ、`y<1375` は常に2（アーチの両足＝閉じた形）、`y≥1380` で
  6程度（歯が分離＝この先は塗り不要）に変わることを確認し、フラッドフィル対象を
  `y=1150-1375` に限定した。
- **フラッドフィルが何も塗らない不具合**: `Image.fromarray(...)` で作った画像に対して
  そのまま `ImageDraw.floodfill` を呼ぶと、`getpixel` で見る限り何も変化しないという
  PIL の既知の挙動に遭遇した（`fromarray` 直後の画像はバッファ共有等の理由で
  floodfill の書き込みが反映されないことがある）。`Image.fromarray(...).copy()` と
  一度複製してから floodfill することで解決した（`prep_user_parts.py` にコメント記載）。
- **眼鏡フレーム下端の破片が口髭パーツに混入**: 眼鏡と口髭の境界（`y=1150`）に完全な
  空白行が無く、眼鏡フレームの下端の破片が `y<1171` あたりまで口髭側に残った。行ごとの
  run のx位置を実測し、破片（`x<650` または `x>850`）だけを選択的に消して、中央の
  口髭アーチ頂点（`x=650-850`）だけを残した。

### 検品メモ

- `icon3-1.png` / `icon3-2.png` を Read で開き、①ウォーターマークの痕が無いこと
  （帽子ドーム中央を拡大して確認）②パーツが浮いていないこと（大きさ・位置）③円形
  トリミング（`-circle.png`）で帽子が極端に欠けないこと、を確認した。
- ウォーターマーク: 完全に除去できた（拡大確認済み、文字の痕跡なし）。
- 角帽: 3回の調整（`TOP_MARGIN` 拡張→効果なし→拡縮率と合わせ先 y を再調整）を経て、
  800×800 の正方形キャンバス内には完全に収まる。円形トリミングでは板の左右の角
  （v1/v2 と同様）に加え、上端もやや欠けるが、ボタン・タッセル本体・板の大部分は残るため
  「極端な欠け」ではないと判断した。
- 丸眼鏡: 元画像どおりの細いフレームのまま使用（アルファ膨張なし）。両目の中心・幅に
  正しく合っている。
- 口髭: 白い内側＋黒輪郭で、口を覆う設計どおりに鼻の下から口にかけて配置。歯（scallop）
  の下端が顎のラインに自然に収まっている。
- **妥協点**: 角帽はドームが縦に長い意匠のため、v1/v2（板幅340px相当）より一回り小さい
  板幅（約251px相当）で妥協した。元画像がJPEGの下端で口髭の歯部分を切っており、
  その復元はできない（歯の最下端は画像の物理的な端で打ち切られている）ため、
  フラッドフィルの範囲をアーチ部分に限定する対処で不具合を回避した。

## v2: フリー素材合成版

生成スクリプト: `make_icon2.py`（v1 の `make_icon.py` を `import` して顔ランドマーク・
背景生成・白縁付けを共有し、小物の合成部分だけを新規実装）。
```
C:\Users\shuya\Projects\script-to-video\.venv\Scripts\python.exe make_icon2.py
```

### 出力

| ファイル | 内容 |
|---|---|
| `icon2-1.png` / `icon2-1-circle.png` | 案1: 角帽＋丸眼鏡＋口髭 |
| `icon2-2.png` / `icon2-2-circle.png` | 案2: 角帽＋丸眼鏡＋白いあごひげ |
| `icon2-3.png` / `icon2-3-circle.png` | 案3: 丸眼鏡＋口髭のみ（帽子なし） |

`icon2-N.png` は 800×800 RGB。`icon2-N-circle.png` は円形トリミングの検品用プレビュー
（RGBA）。表情は案1・案2が `smug.png`、案3が `normal.png`。背景（黒板）・白縁
（`add_paper_outline`, 12px）・800×800 キャンバスへの配置は v1 と同じロジック
（`make_icon.py` の `board_background` / `add_paper_outline` / `T_SCALE` 等）を再利用している。

### 使用素材と出典（すべて商用利用可・帰属表記不要のライセンス）

保存先: `C:\Users\shuya\Projects\assets-kamishibai\icon-parts\`（Git 管理外）。

| パーツ | ファイル | 出典 | 作者 | ライセンス | 取得日 |
|---|---|---|---|---|---|
| 角帽 | `hat-pixabay-2298201.png` | https://pixabay.com/vectors/graduation-cap-icon-clipart-vector-2298201/ | Mohamed_hassan (Pixabay) | Pixabay Content License（商用可・帰属不要） | 2026-09-05 |
| 丸眼鏡 | `glasses-openclipart-275579.png` | https://openclipart.org/detail/275579/glasses（"simple round glasses"） | pawnk | Public Domain / CC0（Openclipart） | 2026-09-05 |
| 口髭 | `mustache-openclipart-237353.png` | https://openclipart.org/detail/237353/mustache-silhouette | GDJ | Public Domain / CC0（Openclipart） | 2026-09-05 |
| あごひげ | `beard-openclipart-233589-crop.png`（元画像 `beard-openclipart-233589-source.png` からひげ部分のみ切り出し） | https://openclipart.org/detail/233589/goatee-silhouette（"Goatee silhouette"。髪・眼鏡・ひげが分離した素材集で、ひげ部分だけを使用） | dear_theophilus | Public Domain / CC0（Openclipart） | 2026-09-05 |

**候補として検討したが不採用のもの**（画風のミスマッチ・写実的すぎる・グラデーション過多などが理由）:
- 角帽: `hat-openclipart-1925.png`（rickvanderzwet, Openclipart #1925, "Silhouette of Mortarboard"）— 輪郭が筆致調で線が荒く、立ち絵のフラットな塗りと合わなかった。
- 角帽: `hat-openclipart-202668.png`（pnx, Openclipart #202668, "Academic cap"）— グレーのグラデーション影付きで写実寄り。
- 角帽: `hat-pixabay-309661.png`（Clker-Free-Vector-Images, Pixabay #309661）— 赤いタッセルで統一感が薄く、`2298201` の方がフラット＋輪郭線の画風に近かったため次点扱い（ファイルは参考として保持）。
- 角帽: `hat-openclipart-1925`（Openclipart #1925, "MOOC U" の文字入りバリエーション）— 文字入りのため不採用。
- 眼鏡: Openclipart "Glasses Face" (#341549, 210enferno) — 唇付きの顔アイコンで単体パーツとして使えなかった。
- 眼鏡: Pixabay 各種（"round glasses clipart icon" 等で検索）— ヒットの大半が無関係（マティーニグラス・双眼鏡等のノイズ）で、単体の丸眼鏡クリップアートが見つからなかったため Openclipart へ切り替えた。
- 口髭・あごひげ: `beardsheet-pixabay-33166.png`（Clker-Free-Vector-Images, Pixabay #33166, "types of beards"）— 19世紀の版画調で線が過密（クロスハッチング）、写実的すぎるため不採用。

### 素材の加工（`make_icon2.py`）

- **角帽**（`process_hat`）: 元画像は青い板＋紺の帯＋黄色いタッセル＋半透明のドロップシャドウ
  付き。①アルファ 150 未満の半透明ピクセル（影のぼかし）を透明化して影を除去、②タッセルの
  黄色以外（青・紺・黒）は輝度を保ったまま黒系（RGB 10〜48 程度）へ落とし、板と帯の明暗差は
  そのまま濃淡として残した。
- **丸眼鏡**（`process_glasses`）: 元画像はレンズ内側が不透明な白塗り＋つる（テンプル）が
  大きく宙へ跳ね上がる誇張されたデザイン。①`GLASSES_CROP` でつるの大部分を切り落とし
  レンズ＋ブリッジ＋つけ根だけを残す（跳ねたつるをそのまま使うと頭髪に黒い枝のような線が
  刺さって見える不具合が出た）、②レンズ内側の白い塗りを透明化してスプライト自身の目を
  透かして見せる。
- **口髭**: 加工なし（元から黒一色・輪郭線不要の意匠のため）。
- **あごひげ**（`add_outline_to_flat_shape`）: 元画像は黒一色のベタ塗りで輪郭線がないため、
  アルファを `MaxFilter` で膨張させた範囲をダークブラウンで塗って輪郭を作り、その上から
  塗り色を白（`#FAF8F2`）に変更したシルエットを重ねた。素材にもとから空いている「口の穴」
  （透明領域）はそのまま活かし、スプライト側の口の位置に正確に重ねることでキャラ自身の口が
  透けて見えるようにした。

### パーツの位置合わせ（アンカー座標）

各パーツ画像を等倍グリッド画像に焼き込んで目視計測し、パーツ内の基準点（アンカー）を
決めた上で、v1 と同じ顔ランドマーク（下表）へ合わせて拡縮・配置した（`make_icon2.py` の
`place_part`）。

| パーツ | アンカー（素材内座標） | 意味 | 合わせ先 |
|---|---|---|---|
| 角帽 | (615, 830) | 帽体の開口部（頭が入る部分）の中心 | `(HAT_CX, 115)` |
| 丸眼鏡 | (900, 305)（`GLASSES_CROP` 後の座標） | 左右レンズ中心の中点 | 両目中心の中点 |
| 口髭 | (930, 45) | 上端中央の谷（鼻の下に当たる点） | `(FACE_CX, MOUTH.y - 14)` |
| あごひげ | (335, 125) | 素材内の「口の穴」の中心 | `MOUTH`（口の中心） |

拡縮率は主要な間隔（丸眼鏡は左右レンズ中心の距離、角帽・口髭・あごひげは目視で適切と
判断した幅）を元にスカラー倍率として算出（コード中の `*_SCALE` 定数）。

### つまずいた点と対処

- **丸眼鏡が顔から浮いて見える不具合**: 最初の実装ではレンズ内側の不透明な白塗りをそのまま
  使ったため、目を覆う大きな白い楕円として描画されてしまった。`process_glasses` で白を
  透明化して解決。
- **丸眼鏡のつるが頭髪に突き刺さって見える不具合**: 元素材のつるが極端に宙へ跳ね上がる
  意匠だったため、そのまま使うと黒い曲線が髪の中を貫通するように見えた。`GLASSES_CROP`
  でつるの大部分を切り落として解決。
- **あごひげの穴から首元が透けて見える不具合**: 初回はあごひげの「上端」をアンカーにして
  口の少し下へ配置したが、素材内の口の穴の位置とずれ、穴からスプライトの首・リボンが
  透けて見えてしまった。アンカーを穴の中心そのものに変更し、`MOUTH` 座標へ正確に重ねて解決。

### 妥協点・検品メモ

- 3案とも Read ツールで目視し、①素材が浮いていない（大きさ・位置・向き）②画風の違和感が
  小さい ③円形トリミングで帽子が極端に欠けない、を確認済み（`icon2-N-circle.png`）。
- **画風差**: 立ち絵は手描き線画（線の強弱・筆致の揺らぎあり）だが、拾った素材はベクター
  クリップアート（線の太さが均一）で、特に丸眼鏡・角帽の輪郭線は立ち絵よりやや太く・硬い。
  「フリー素材を使う」という指示の性質上、完全な画風統一は狙わず、フラットな塗り＋濃い
  輪郭線という大枠が合っていることを基準に選定した。
- 角帽は元画像の遠近感（下から見上げたアングル）をそのまま活かしており、キャラの正面向き
  の顔に対してやや斜めに乗っている見た目になる。指示の「少し傾けてよい」の範囲内と判断し、
  補正しなかった。
- 案2のあごひげは素材の縁（穴のフチ）が上唇のすぐ下に接するため、鼻先〜上唇のごく一部に
  ひげが被る。口そのもの（ω の形）は完全に見えている。
- 口髭は「ちょび髭」向けの素材が見つからず、市販クリップアートで一般的な小さめのカール
  髭（GDJ 作）を縮小して代用した。厳密なトゥースブラシ型ではないが、口を覆わない小さな
  髭という要件は満たしている。

## v1: 手描き版（参考として保持）

生成スクリプト: `make_icon.py`（実行は script-to-video の venv の python）。
```
C:\Users\shuya\Projects\script-to-video\.venv\Scripts\python.exe make_icon.py
```

コンセプトは同上。小物（学者帽・眼鏡・髭）はすべて Pillow の図形描画で手描きしている
（v2 との違いはここのみ）。

### 出力

| ファイル | 内容 |
|---|---|
| `icon-1.png` / `icon-1-circle.png` | 案1: 角帽＋丸眼鏡＋白いあごひげ |
| `icon-2.png` / `icon-2-circle.png` | 案2: 丸いドクターキャップ（タム帽）＋半月眼鏡＋白いふさふさの口髭＋あごひげ |
| `icon-3.png` / `icon-3-circle.png` | 案3: 角帽（案1と同じ）＋丸眼鏡（案1と同じ）＋黒いちょび髭（軽め） |

`icon-N.png` は 800x800 RGB（YouTube アイコン用）。`icon-N-circle.png` は円形表示（YouTube の
実際の見え方）を確認するための検品用プレビュー（RGBA、円の外側は透明）。

### 各案の詳細

### 案1（icon-1）
- 表情: `smug.png`（得意顔＝学者っぽい）
- 帽子: 角帽（mortarboard）。黒い菱形の板＋頭に沿う黒い帽体＋金色のボタン＋右に垂れる金タッセル。
- 眼鏡: 丸眼鏡。細い黒フレーム、薄い水色がかった白のレンズ塗り＋ハイライト線、両目の中心に合わせた。
- 髭: 白いあごひげ（口の少し下から顎にかけての小さな goatee 型）。口（ω の形）は隠していない。

### 案2（icon-2）
- 表情: `normal.png`（目を開けた自然な顔。半月眼鏡越しに視線が見える構図を意図）
- 帽子: 丸いドクターキャップ（タム帽/ベレー風）。ふくらんだ黒い丸帽＋頭に接する帯＋ひだの
  ステッチ線＋てっぺんの金ボタン。
- 眼鏡: 半月眼鏡（下がった位置）。`ImageDraw.chord` で下半分だけのレンズ形状にし、金縁で
  「読書メガネを鼻先にかけている」印象にした。
- 髭: 白いふさふさの口髭＋あごひげ（輪郭に沿って小さな円を連ねてもこもこ感を出した）。
  `normal.png` は口が小さい線のため、口髭で覆われる（案2は口を隠す設計）。

### 案3（icon-3）
- 表情: `smug.png`（案1と同じ）
- 帽子: 角帽（案1と同一デザイン・同一関数）
- 眼鏡: 丸眼鏡（案1と同一）
- 髭: 黒い小さなちょび髭のみ（あごひげなし）。「軽め」にする指示に沿い、装飾を最小限にした。
  口は隠していない。

### 位置合わせに使った座標

`smug.png` / `normal.png` はいずれも 823×1513 の透過 PNG で、同じ体型・同じ顔位置のリグ違い
（表情差分）。以下は元画像座標系（左上が原点）で、`_grid_*.png`（グリッド線入りの一時ファイル、
確認後に削除済み）を目視計測して確定した値。`make_icon.py` の定数として残している。

| ランドマーク | 座標 (x, y) |
|---|---|
| 左目中心（画面向かって左） | (237, 325) |
| 右目中心（画面向かって右） | (432, 320) |
| 目の半径目安 | 58px |
| 顔の中心線 x | 345 |
| 口（ω）の中心 | (345, 403) |
| 顎先 | (345, 438) |
| 帽子の中心 x（アンテナの谷間） | 350 |
| 帽子の帯が頭に接する y | 90 |

小物は上記の座標を基準に、4倍解像度（`SS=4`）のレイヤーへ描いてから `LANCZOS` で縮小し、
アンチエイリアスを効かせている。

### 背景・縁取り

- 背景は script-to-video の紙芝居モードと同じ黒板（`Kamishibai()` 既定色 `#2E4A3C`）を
  `render_board_plate` / `compute_stage` で実際にレンダリングし、拭き跡・周辺減光つきの
  正方形を切り出して 800×800 に縮小したもの（無地の代用ではなく実際の黒板テクスチャ）。
- 立ち絵の白い縁は `add_paper_outline`（既定の `puppet_outline_px=12`）をそのまま使用。
  紙人形を切り抜いたような見た目にしている。

### 合成の仕組み（つまずいた点と対処）

- 帽子の頂点（角帽の板の先端、タム帽の丸い頭頂）は元スプライトの `y<0` にはみ出す設計だった
  ため、素朴に実装すると小物レイヤーの上端でクリップされ、角帽が「平らに切れた台形」に、
  タム帽が「黒い箱」に見える不具合が起きた（1〜2回目の生成で発生・目視で発見）。
  対処として、元スプライトの上に `TOP_MARGIN=160px` の透明な余白を持つ拡張キャンバスを
  作ってから小物を描き、最終キャンバスへの変換時にその余白ぶんを補正するよう修正した
  （`make_icon.py` の `TOP_MARGIN` / `compose_character` / `build_icon` 参照）。
- タム帽のひだ線は `ImageDraw.arc` の角度指定を誤り、帽子の**上**にはみ出す不具合があった
  （PIL の角度系は 0°=3時方向・時計回りのため、下半分の弧は `start=20, end=160` が正しい）。
  修正して帽体の下側にステッチ状のひだとして収めた。

### 妥協点・検品メモ

- 顔・目・口・髭が隠れていないか、①〜③の観点（顔の要素が隠れていない／小物が顔に合って
  浮いていない／円で切ったときに帽子が極端に欠けない）を `icon-N-circle.png` で確認済み。
  円形トリミングでは角帽の左右の角（板の左端・右端の頂点）がわずかに切れるが、中央の頂点・
  ボタン・タッセル本体は残るため「極端な欠け」ではないと判断した。
- 案2の白いふさふさ髭は `normal.png` の口を覆う設計（口の位置に髭を置く＝指示の許容範囲内）。
  案1・案3の髭は口を覆わない位置にした。
- 小物のスタイル（フラットな塗り＋濃い輪郭線）は立ち絵の線画に寄せたが、Pillow の図形描画
  ベースのため、公式イラストほどの曲線の滑らかさ・筆致の揺らぎは再現していない（アイコンの
  縮小表示では気にならない範囲と判断）。
- 3案とも同じ黒板背景・同じ縁取り設定で統一し、キャラの表情差分（`smug`/`normal`）と小物の
  組み合わせだけを変えている。
