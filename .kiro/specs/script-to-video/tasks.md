# Tasks: script-to-video（台本→機械音声解説動画 生成ツール）

- [ ] 1. プロジェクト基盤の決定と初期化
  - [ ] 1.1 技術スタックを決定する（Open Question の解消。候補比較と決定理由を記録）
  - [ ] 1.2 ツール本体のリポジトリを作成し、本スペック（.kiro/specs/script-to-video/）を移設する
  - [ ] 1.3 VOICEVOX の話者を選定し、クレジット表記の挿入方針（エンドクレジット自動挿入）を決める
    - _Requirements: 2.1_

- [ ] 2. シーン YAML スキーマの定義と Loader
  - [ ] 2.1 シーン YAML のスキーマを定義する（ナレーション・テロップ・検索クエリ・候補 URL + ライセンス・生成プロンプト・スタイル定義・読み修正・ポーズ・出典・BGM 指定）
    - _Requirements: 1.1_
  - [ ] 2.2 バリデータを実装する（欠落・形式エラーをシーン単位で特定して報告）
    - _Requirements: 1.2_
  - [ ] 2.3 サンプル YAML（既存台本1本ぶん）を手書きし、バリデータを通す
    - _Requirements: 1.1, 1.2_

- [ ] 3. 執筆レイヤーの更新（台本リポジトリ側）
  - [ ] 3.1 台本テンプレートを更新し、シーン YAML を Markdown と併せて出力する形式にする（HeyGen CSV 出力は廃止）
    - _Requirements: 1.1, 1.3_
  - [ ] 3.2 執筆ワークフロー（/draft 等）に素材候補収集を組み込む: ストック（Pixabay / Pexels の画像・動画クリップ、商用可・クレジット不要限定）・Commons（PD/CC 確認と出典表記）・汎用検索（出典 URL 記録+権利要確認マーク）・存命人物の注意付記
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_
  - [ ] 3.3 執筆ワークフローに BGM 候補選定（ライセンス確認込み・URL 提示）を組み込む
    - _Requirements: 3.6_

- [ ] 4. 音声パイプライン（TTS）
  - [ ] 4.1 ローカル VOICEVOX エンジンと連携し、シーン別 WAV を生成する
    - _Requirements: 2.1_
  - [ ] 4.2 読み修正を合成に反映する
    - _Requirements: 2.2_
  - [ ] 4.3 ポーズ秒数の位置に無音を挿入する
    - _Requirements: 2.3_

- [ ] 5. 映像レンダリング（ffmpeg）
  - [ ] 5.1 シーン合成を実装する（背景+シーン見出し+テロップを音声長に同期）
    - _Requirements: 6.1_
  - [ ] 5.2 採用素材の配置と Ken Burns 効果（自動ズーム・パン）を実装する
    - _Requirements: 6.1, 6.2_
  - [ ] 5.3 出典テロップの自動表示を実装する
    - _Requirements: 6.3_
  - [ ] 5.4 ナレーション本文から SRT を生成する
    - _Requirements: 6.4_
  - [ ] 5.5 動画素材の合成を実装する（シーン尺へのトリム／ループ、クリップ音声のミュート）
    - _Requirements: 6.5_

- [ ] 6. 出力パッケージング
  - [ ] 6.1 シーン動画を結合して完成 MP4 を出力する
    - _Requirements: 8.1_
  - [ ] 6.2 中間素材（シーン別 WAV・SRT・採用素材 PNG・テロップ素材・タイムライン情報）を汎用形式で構造化保存する
    - _Requirements: 8.2, 8.3_
  - [ ] 6.3 ヘッドレス一気通貫の検証: サンプル YAML（素材選択なし=テロップのみ構成）から MP4 生成まで通す
    - _Requirements: 4.5, 8.1, 8.2_

- [ ] 7. 候補ギャラリー（ローカル Web UI）
  - [ ] 7.1 シーンごとの候補素材一覧（画像・動画）とライセンスバッジ（PD / CC-BY / 商用可 / 権利要確認）を表示し、動画候補はプレビュー再生できるようにする
    - _Requirements: 4.1, 4.2, 4.8_
  - [ ] 7.2 選択操作と、採用画像の即時ダウンロード+中間素材フォルダ保存を実装する
    - _Requirements: 4.3_
  - [ ] 7.3 再検索（クエリ編集込み: ストック / Commons / 汎用）を実装する
    - _Requirements: 4.4_
  - [ ] 7.4 「素材なし（テロップのみ）」の選択肢を実装する
    - _Requirements: 4.5_
  - [ ] 7.5 権利要確認画像の採用時に出典 URL 記録+出典テロップ生成を必須化する
    - _Requirements: 4.6, 6.3_
  - [ ] 7.6 選択結果を記録ファイルに保存し、再レンダリング時に承認をスキップできるようにする
    - _Requirements: 4.1_

- [ ] 8. 生成 AI フォールバック
  - [ ] 8.1 画像生成 API のプロバイダを選定する（商用利用条件の確認込み。Open Question の解消）
    - _Requirements: 5.1_
  - [ ] 8.2 ギャラリーに「生成 AI で作る」操作を実装する（明示操作なしに API を呼ばない）
    - _Requirements: 4.7, 5.1_
  - [ ] 8.3 スタイル定義+シーン別プロンプトの連結によるプロンプト構成を実装する
    - _Requirements: 5.2, 5.3_

- [ ] 9. BGM ミキシング
  - [ ] 9.1 BGM のループと -14 LUFS ラウドネス正規化を実装する
    - _Requirements: 7.1, 7.2_
  - [ ] 9.2 ダッキング（ナレーション中の BGM 音量自動低下）を実装し、パラメータを実測調整する
    - _Requirements: 7.3_
  - [ ] 9.3 BGM 未指定時はナレーションのみで構成する
    - _Requirements: 7.4_

- [ ] 10. E2E 検証（実台本での通し運用）
  - [ ] 10.1 既存台本1本を執筆レイヤー込みでフル変換し（候補収集→ギャラリー承認→レンダ→BGM）、完成動画を確認する
    - _Requirements: 1.3, 9.1, 9.2_
  - [ ] 10.2 中間素材を AviUtl 等で読み込み、再編集できることを確認する
    - _Requirements: 8.3_
  - [ ] 10.3 VOICEVOX クレジット表記が動画に含まれることを確認する
    - _Requirements: 2.1_

## Open Questions

- タスク 1.1（技術スタック）・1.3（話者）・8.1（画像生成プロバイダ）は着手時に決定が必要（requirements / design の Open Questions と対応）
- 解像度・アスペクト比は 1080p / 16:9 仮置きのままタスク 5 で実装し、変更要件が出たら見直す
- 将来拡張（完全自動モード、動画生成、.exo 出力、クラウドレンダ）は本タスクリストの対象外
