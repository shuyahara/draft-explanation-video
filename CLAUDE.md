# draft-explanation-video

機械音声（TTS）解説動画の **台本** を作成するプロジェクト。

> **HeyGen 運用は凍結（2026-08-14）**。新規台本は自作レンダリングツール
> [script-to-video](https://github.com/shuyahara/script-to-video) 向けに執筆する。
> 詳細は後述「script-to-video 向け出力」。
>
> **凍結中の旧運用**: 台本は HeyGen（AI Studio / Creator プラン・手動運用）に流し込み、
> AI アバター＋機械音声で読み上げていた（API は使わず、Web エディタへのシーン単位貼り付け／
> CSV 一括インポート）。過去台本（`scripts/` 内の HeyGen 期のもの）の背景として、
> HeyGen 関連の記述は本ドキュメントに簡潔に残す。

## このプロジェクトでの Claude の役割

ユーザーが与えたテーマについて、**論拠を積み上げて結論を導く** 解説動画の台本を執筆する。
単なる情報の列挙ではなく、「問い → 根拠 → 反証の検討 → 結論」という論証構造を持たせる。

**台本のナレーション本文は、オーケストレーター（Fable）本人が執筆・改稿する**（2026-08-24
ユーザー指示。グローバル設定の「執筆はワーカー委譲」より本ルールが優先）。調査・裏取り・
シーン YAML の組み立て・素材候補収集・レンダリングなどの周辺作業はワーカー委譲のままでよいが、
読み上げ文の創作・推敲はワーカーに委譲しない。ワーカーが YAML 化などで文言変更が必要になった
場合は、変更案をオーケストレーターに差し戻して判断を仰ぐ。

## 動画・台本の前提

- **尺**: 1本あたり 10〜30 分を想定。
- **読み上げ速度の目安**: 日本語 TTS はおよそ **300〜360 文字/分**。
  - 10 分 ≒ ナレーション 3,000〜3,600 文字
  - 20 分 ≒ ナレーション 6,000〜7,200 文字
  - 30 分 ≒ ナレーション 9,000〜10,800 文字
  - 台本作成時は冒頭に想定尺と概算文字数を明記し、本文の文字数を合わせる。
- **音声**: 機械音声で読み上げる。後述の「機械音声向けの書き方」を必ず守る。

## 台本フォーマット（ナレーション＋画面指示）

シーン単位で「読み上げ文」と「画面指示」を併記する。`templates/script-template.md` を雛形とする。
**1シーン＝シーン YAML の `scenes[]` の1件**に対応する（音声・字幕・章の単位）。ただし
**v6 以降、シーン内の画面は `beats[]`（ビート）単位で複数回切り替わるのが標準**であり、
「1シーン＝1枚の主視覚」という旧来の粒度ではない（詳細は後述「ビート運用」）。
読み上げ文は目安として **約1,000文字以内**（旧 HeyGen 運用のセグメント上限に由来する目安だが、
シーンの粒度として引き続き有効なため維持する。長くなる場合は論証の区切りでシーンを分ける）。

```
## シーン{N}: {見出し}

**ナレーション**
{シーン YAML の narration にそのまま転記するプレーンテキスト。約1,000字以内。
 記号・マークダウン・ルビ括弧を入れない（そのまま読み上げられるため）}

**画面**
- テロップ: {画面に出す短い文言}
- ビジュアル: {図・グラフ・映像・素材のメモ}
- 出典表示: {引用がある場合は出典をテロップ表示する指示}
- 発音・ポーズメモ: {誤読しやすい語の読み・区切り、間（ま）を入れたい箇所}
```

全体構成の基本形:
1. **フック（つかみ）** — 視聴者の関心を引く問い・意外な事実・結論の予告。冒頭15秒以内に
   この動画が答える問いを言語化する。
2. **問題提起** — 何を、なぜ問うのか。
3. **論証本体** — 根拠を1つずつ提示。各根拠に出典をつける。反証・反論も検討する。
4. **結論** — 積み上げた論拠から導かれる結び。冒頭で言語化した問いに明示的に答えて閉じる。
5. **まとめ／次への誘導** — 要点の再確認。

**章の設計**（論証型・探索型いずれの構成でも共通）:
- 各章末は次章へつながる問いで終える（「では〜はどうなのか」等）。視聴者の関心を章をまたいで
  引っ張る。
- 同一構文の反復を3回以上作らない。特に研究紹介（「〜年、〜らは〜と報告した」型）は結論先出し・
  当事者視点・数字先出しなど語り口を混ぜ、同じ構文が3件連続しないようにする。

**代替の全体構成: 探索型**（v6 で試用。尺は8〜10分目安。論証型の代わりに発見の過程を
追体験させたい場合に選ぶ）
1. 具体的な一人の場面で開く（フック）。
2. その場面を問いへ変換する。
3. 問いを調べていく過程として研究を提示する（結論を先に言わない）。
4. 中盤で前提を反転させる（「実は〜だった」）。
5. 冒頭の場面へ戻り、問いに答える。

## 引用・ファクトの扱い（出典必須・検証あり）

解説の説得力は引用の正確さに依存する。**捏造は厳禁**。

- 論文・実験・統計・著名人の言葉・名言を引用する場合、必ず **出典**（著者／タイトル／発表年／可能なら URL）を添える。
- 引用する事実は **WebSearch / WebFetch で裏取り** する。記憶だけに頼らない。
- 裏取りできなかった引用は **台本に載せない**、もしくは「要確認」と明示してユーザーに判断を委ねる。
- 名言は「誰の言葉か」を確認する（誤帰属が多い領域なので特に注意）。
- **研究・分析・調査を紹介するときは、実施機関と実施者をなるべくナレーション本文で明記する**
  （「どこの大学の・誰の研究か」。2026-08-24 ユーザー指示）。特に大学名自体に権威がある場合
  （ハーバード、イェール、東大など）は必須。ただし帰属は一次資料で確認できたものだけを書き、
  確認が不十分な場合は「〜などの研究チーム」のように含みを持たせる（不正確な権威付けは逆に
  信頼を損なう）。語り口は反復回避規約に従い主体先出し・数字先出し・結論先出しを混ぜてよいが、
  帰属自体は各研究につき必ず一度は述べる。
- 出典は台本内に脚注的にまとめ、かつ画面指示の「出典表示」にも反映する。

## 機械音声向けの書き方（VOICEVOX 前提）

話者は **青山龍星**（VOICEVOX）固定。台本のナレーションは、シーン YAML の `narration`
セグメントとして機械可読に記録する（詳細は後述「script-to-video 向け出力」）。

- **読み上げテキストはプレーンに**。記号・マークダウン・ルビ括弧は入れない
  （そのまま読み上げられるため）。
- **読み修正**は YAML の `readings`（`surface`＝表記／`reading`＝カタカナ）で機械可読に指定する。
  誤読しやすい語は、台本本文・「発音・ポーズメモ」にも読みをメモしつつ、シーン YAML に
  必ず `readings` エントリを添える。
- **ポーズ（間）**は `narration` セグメントの `pause_after`（秒、機械可読な数値）で指定する。
  間を入れたい箇所でナレーションをセグメント分割し、直前のセグメントに `pause_after` を付ける
  （省略時はポーズなし。台本の「発音・ポーズメモ」にも入れたい箇所を書いておく）。
  **3段階以上を使い分ける**（全箇所を同じ長さにしない）。目安: 短い区切り 0.3〜0.5秒／
  論点の切れ目 0.6〜0.9秒／決め文（言い切り）の直後 1.0〜1.2秒。
  **`pause_after` 省略時のセグメント境界には、エンジンの前後無音マージン由来の約0.26〜0.29秒
  しか間が入らない**（VOICEVOX 側のパラグラフ内句読点ポーズ〔`pause_scale` が効く〕とは別物で、
  こちらは常に短い）。文境界でセグメントを分割する場合は、単なる文の連続で間を詰めたい
  ときでも `pause_after` を省略せず、最低 0.2 秒程度を明示する（実測で約0.45〜0.5秒になり、
  「間が短い」と感じられる水準を避けられる。R20 の文間無音実測で判明。
  references/20260820-narration-gap-fix.md 参照）。
- **二人称の問いかけを各章1回以上入れる**（例:「あなたは〜と感じたことはないだろうか」）。
  会話体・二人称での語りかけが学習効果を高めるという Mayer のパーソナライゼーション原理を
  根拠とする（Moreno, R., & Mayer, R. E. (2000). Engaging students in active learning: The
  case for personalized multimedia messages. *Journal of Educational Psychology*, 92(4),
  724–733.；総説として Mayer, R. E. (2009). *Multimedia Learning* (2nd ed.), Cambridge
  University Press）。ただし「各章1回以上」という頻度自体は本規約側の設計判断であり、
  原論文が定める基準ではない。
- **クレジット表記**「VOICEVOX:青山龍星」は script-to-video がエンドクレジットとして
  自動挿入するため、台本・YAML 側で個別に表記する必要はない。
- 1文を長くしすぎない。句読点で TTS の間（ま）を設計する。
- 同音異義語で誤解が生じる表現は避け、文脈で一意に読めるようにする。
- 視覚に依存する表現（「左の図」「赤い線」等）はナレーション単体で意味が通るよう補う。
- 箇条書きや記号は画面指示側（YAML では `telop`）へ。ナレーションは「話し言葉として自然な
  地の文」にする。

> **凍結中の旧運用（HeyGen 前提）**: HeyGen はスクリプト欄の文字をそのまま読み上げるため、
> `API（エーピーアイ）` のようなルビ括弧は不可だった（括弧の中身まで読まれる）。読み調整は
> 単語を右クリックする「発音（Pronunciation）」機能、ポーズは Pause ボタン（0.5秒刻み）で
> 行っていた。過去台本（HeyGen 期のもの）を参照する際の背景情報として残す。

## script-to-video 向け出力

台本（Markdown）を人間用マスターとし、レンダリング指示書として **シーン YAML** を併せて出力する。
YAML はレンダリングツール [script-to-video](https://github.com/shuyahara/script-to-video) の
入力であり、ツールはこの YAML だけを読んで実行時に決定的に動画を生成する（LLM は呼ばない。
検索語・読み・プロンプトはすべて YAML に確定させておく）。

- **YAML の仕様は script-to-video 側が正**。本リポジトリでは仕様を複製しない。
  - ローカル: `C:\Users\shuya\Projects\script-to-video\docs\schema.md`
  - GitHub: `shuyahara/script-to-video` リポジトリの `docs/schema.md`
  - サンプル: `script-to-video/examples/sns-lookism-sample.yaml`
  - ビート運用（`beats[]`。v6 以降の標準）の設計判断は同リポジトリの
    `docs/design-beat-rendering.md` が正。詳細は後述「ビート運用」。
- **保存先**: `scripts/{台本と同名}.yaml`。雛形は `templates/scene-yaml-template.yaml`。
- 未知のフィールドはツール側でエラーになる（`extra="forbid"`）。仕様にない項目は書かない。
  仕様に不足を感じた場合は独自拡張せず、まずユーザーに相談する。
- 完成した YAML は script-to-video の `validate` サブコマンドで検証する。
  ```
  cd C:\Users\shuya\Projects\script-to-video
  .venv\Scripts\python.exe -m script_to_video validate <yamlパス>
  ```
- 出典は音声で読み上げず、原則 `telop`（画面テロップ）で表示する（読み上げたい場合はナレーションに含める）。

### 図解スクリプト（`diagram`）

抽象概念・因果関係・比較・手順・データは、実写素材ではなく **図解**（アイコン・ラベル・矢印・
棒/折れ線をモーション付きで組み上げる説明背景）で表現できる。書くのは「型」「要素の並び」
「id・ラベル」「どのセグメントで出現するか（`at`）」だけで、位置・サイズ・色・アニメーションの
数値は一切書かない（レイアウトと動きはツール側が決定的に計算する）。仕様の詳細（型ごとの
フィールド・上限・バリデーション）は `docs/schema.md` の `diagram` 節が正。ここでは執筆判断の
要点だけをまとめる。

**ビジュアル意図を最初に書く**（Requirement 16.1）

シーンごとに、図解・検索クエリ・生成プロンプトを決める**前に**「どんな絵で見せたいか」を
1〜2文で決める。先に手段（図解 or 素材）を選んでから理由を後付けすると、「図解のための図解」
や「画面が寂しいだけのシーン」になりやすいため、必ず意図が先。

- 決めたビジュアル意図は、台本 Markdown の「ビジュアル」欄と、シーン YAML の `visual_intent`
  の両方に同じ内容で書く（`visual_intent` はレンダリングには使わず、候補ギャラリー表示にのみ
  使う短い自由文。仕様は `docs/schema.md` の `visual_intent` 節）。
- `diagram` の型・`search_query`・`gen_prompt` は、このビジュアル意図から導く。

**画面構成は3種類**（Requirement 16.2。専用フィールドではなく `diagram` と `assets` の有無の
組み合わせで表す。旧仕様の「図解 or 実写の択一」を、背景付き図解を加えて発展させたもの）

| ビジュアル意図の傾向 | 画面構成 | YAMLの書き方 |
|---|---|---|
| 情景描写・具体的な人／物／場所・フック（つかみ）・エピソード | 全画面実写 | `assets` のみ |
| 論証・手順・比較・データを図で見せたい。情景の支えは不要（画面が持つ） | 図解のみ（無地下地） | `diagram` のみ |
| 論証・手順・比較・データが軸だが、画面が寂しい／情景の空気感も一緒に見せたい | **背景付き図解** | `diagram` ＋ `assets` を併記（排他ではない。素材にぼかし＋暗色スクリムをかけて図解を重ねる） |

- **抽象概念のシーンでも「情景の支えが欲しい」と判断したら背景付き図解を選ぶ**
  （例: 定義や構造を示す図解の背後に、街の雑踏など情景の空気感を敷く）。
- 迷ったら無地下地から検討する。結論・まとめなど**引き締めたい場面は無地下地のままでよい**
  （背景を足すこと自体を目的化しない）。
- **説明イラストは「説明イラスト第一」で考える**（詳細は後述「素材候補収集ワークフロー」）。
  「実験で参加者が写真を見比べている」のような説明的な情景は、ストック検索ではまず出てこない
  ため、`gen_prompt` を先に起草してからストックで代替できるか判断する。一般名詞的な情景
  （街の雑踏、スマートフォン操作など）や、実写であることに意味がある画は引き続きストックを
  優先する。ギャラリーでの明示操作・課金承認制は変わらない。
- **非ビート運用（v5互換）**では、論証型の台本は図解（無地下地／背景付き問わず）が主体になるが、
  情景の情報量は実写に残す。目安として図解シーンは全体の3〜5割（全部を図解にすると単調になる）。
- **ビート運用**では、この比率はシーン単位ではなく**ビート単位**で考える。R19 のベンチマークが
  示す画面様式は「生成AIの一枚絵（`image` ビート）を主役に据え、列挙・因果・比較など構造的な
  主張の場面だけ `diagram` ビートに切り替える」。図解ビートを連続させすぎない（旧来の「ピルの
  羅列」に戻ってしまうため）。全体としての目安は変わらず3〜5割程度が図解が占める形。

**型の選び方**

| 台本での役割 | 型 | 目安 |
|---|---|---|
| 状態が変わることが主張を運ぶ（1つの特徴が波及／論拠が結論へ収束／選別が起きる） | `narrative` | `layout: radiate`/`converge`/`row`。**ビート運用（`beats`）での第一候補** |
| 主張を積み上げる／原因から結果へ／前提から帰結へ | `buildup` | 3〜12要素・5行まで |
| 実験の手順・時間順に進む処理・ステップの説明 | `flow` | 3〜6ステップ |
| 「AとBはどう違うか」「短期と長期」「従来と提案」 | `comparison` | 対比5組まで |
| 裏取り済みの数値を見せる（推移・大小の比較） | `chart` | barは1系列3〜8点／lineは3系列まで・各3〜10点 |

迷ったら、ビート運用（`beats`）では `narrative`、非ビート運用（v5互換）では `buildup`。
**ビート運用では旧来のピル型（`buildup`/`flow`/`comparison`）は `chart` を除いて非推奨**
（存置はするが新規シーンの第一候補にはしない。`validate` が `beats` 内のピル型使用を情報行で
知らせる。`chart` は裏取り済み数値の提示に引き続き必須なので対象外）。
型が合わない図を無理に押し込むより、シーンを分けて2つの図解にする方がよい。

**`narrative` の3レイアウト**（ピルを使わず、ピクトグラム・下線・極太タイポで状態変化を見せる）

| layout | 場面 | 件数 |
|---|---|---|
| `radiate` | 中心の1特徴から他の評価へ広がる（例: 外見の魅力→温かい・社交的・能力が高い…と波及） | items 3〜6件 |
| `converge` | 複数の論拠が1つの結論へ収束する | items 2〜4件 |
| `row` | 同型の要素が並び、一部だけ選ばれる／沈む（選別） | count 4〜10 |

ラベルは12文字以内（`result` は16文字・`caption` は20文字）。座標・色・アニメーションは書けない
（既存型と同じ制約）。ナレーション中の列挙を語順に合わせて出したいときは、要素に `after`
（そのキュー本文にちょうど1回だけ現れる部分文字列）を添える。文字数比で按分した時刻に出現する
（省略時はキューの開始時刻）。

**書き方の手順**

1. 先にナレーションを書く。次に「どの文で何が新しく登場するか」を決める。
2. その切れ目でナレーションのセグメントを分ける。図解の出現粒度はセグメント粒度で決まる。
   **文の途中では分けない**。
3. 型を選ぶ（上表）。
4. 図解の最終形（全要素が出た状態）を書く。
5. 各要素に `at: <セグメント番号>`（1始まり）を付ける（`chart` は系列単位）。
6. 章が変わるところで `key_color` を変える（任意）。**`key_color` は明度（L）のみが読まれ、
   5段階（tone -2〜+2）に量子化されて下地の明度差として反映される**（色相・彩度は読まれない。
   全編の色相・彩度はツール側で固定）。章の識別は章カード（`chapter_title`）が本線で、
   `key_color` の明度差はあくまで補助。同じ章のシーンには同じ値を書く。

**守るルール（チェックリスト）**

- [ ] ラベルは12文字以内（体言止め・キーワード。文を入れない。ナレーションの言い換えであって写しではない）。
- [ ] 座標・サイズ・色・アニメーションは一切書かない。書けるのは `key_color`（`#RRGGBB`）だけで、それも明度（L）しか使われない（色相・彩度・下地色はツールが固定する）。
- [ ] `buildup`: 1行4要素まで・5行まで・合計12要素まで・矢印12本まで。矢印は隣接する行の間で引く。
- [ ] `flow`: 3〜6ステップ。矢印は自動で引かれるので書かない。分岐は表現できない（必要なら `buildup` へ）。
- [ ] `comparison`: 対比5組まで。`accent`（`left`/`right`/`both`）は本当に言いたい対比点だけに付ける（全部に付けると強調が消える）。
- [ ] `chart`: 数値は必ず裏取り済みのものだけ（推計値・概算は使わない）。`source` に出典を書く（画面下に小さく焼き込まれる。音声では読まない）。**動画1本につき `chart` は1箇所まで**（多用するとデータ動画のように単調になる）。
- [ ] `narrative`: `radiate` items 3〜6件／`converge` items 2〜4件／`row` count 4〜10。ラベル12文字以内（`result`16文字・`caption`20文字）。`after` はそのキュー本文にちょうど1回だけ現れること（0回・2回以上はエラー）。
- [ ] 強調（`accent: true`）は1シーンに1箇所を目安に。結論・キーワード・対比点に使う。
- [ ] アイコン名は Material Symbols（https://fonts.google.com/icons ）の名前をスネークケースでそのまま書く。迷ったら `validate` が候補を出すので、まず書いて検証する。
- [ ] 図解はナレーションを読んでいれば理解できる補助であること。図を見ないと分からない情報を図だけに置かない。

**セグメント分割のコスト**: TTS はセグメントごとに個別合成して連結するため、分割点にはエンジン
既定の前後無音が入る。文の切れ目で分ける限り自然な間になるが、意味のない細切れ分割はテンポを
悪くする。図解の出現が必要な箇所だけ分ける。

### ビート運用（`beats[]`。v6 以降の標準）

v6 以降、シーンの映像トラックは `beats[]`（ビート＝画面が変わってから次に変わるまでの区間）で
構成する。旧来の「1シーン＝1枚の主視覚＋1つの図解」という粒度をやめ、シーン内で `image`
（一枚絵）・`diagram`（図解）・`typo`（強調カット）・`chapter`（章カード）の列として画面を
設計する。仕様は `docs/schema.md` の `beats[]` 節、設計判断の背景は
`docs/design-beat-rendering.md` が正（本節は執筆判断の要点だけをまとめる）。

- **v5（凍結中の旧台本）は無改変**。`beats` を書かないシーンは従来どおりのレンダリング経路
  （実写／図解の単一主視覚）で動く。移行の強制はない。
- `beats` を使うシーンでは、シーン直下の `assets` / `gen_prompt` / `search_query` / `diagram` /
  `diagrams` / `cutaways` は書けない（エラー）。主視覚・図解・インサートはすべて対応する
  ビートが持つ（一部区間だけに被せるインサートを表現したい場合は `image` ビートを分ける）。
  `telop` / `key_color` / `chapter_title` / `chapter_bgm` / `emphasis` / `readings` /
  `visual_intent`（シーン直下）は挙動不変（音声・章・色の正本のまま）。

**ビート境界は「意味の節目」に置く**

- 字幕キューの機械的な分割（1文＝1カット）ではなく、**話題・主張・場面が変わるところ**で切る。
  同じ話題を掘り下げている間は1枚を保持する。
- 一枚絵の保持は **8〜15秒を目安**にする。20秒を超えると `validate` が警告する
  （「同じ絵が長すぎます。話題の節目でもう1枚に分けるか、図解を挟んでください」）。
- **大きな転換（章の変わり目・場面転換）はセグメント境界（ポーズがある位置）に合わせる**。
  同じ段落内の細かい切り替えはキュー境界で構わない。精度が要る箇所はナレーション自体を
  `pause_after` で分ける。
- **ビート境界は字幕キュー（≒文）境界にしか置けない**。1枚の保持が10秒を超えそうな長い1文は、
  執筆段階でナレーションを2文に分けてキュー境界を作っておく（後から分割できなくなるため。
  R19 実台本検証 19.12 で判明）。
- 図解の決め要素（結論ラベル・光の輪など）の `after`（語同期）は**句の終端語に置かない**。
  `after` は「その語の終わり」の時刻なので、キュー末尾の語を指定すると要素の保持がほぼゼロに
  なる。決め要素は終端の1〜2秒手前の語に同期させる。
- 各ビートに **`cut_reason`（ここで画を切り替える理由）を必ず書く**。レンダリングには使われず
  `validate` とギャラリーが表示するだけだが、後から「転換が早すぎる」等の判断をこの欄だけで
  やり直せる。書き方は「【何が起きる場面か】なぜここで束ねる／切るか」の一言（例:「【学問的
  背景の提示】キュー1〜3は同じ『知見を紹介する』動作なので1枚で持たせる」）。

**ビート種別の使い分け**

| 種別 | 用途 | 執筆メモ |
|---|---|---|
| `image` | 一枚絵。2.5Dパララックス／ズームでモーションを付ける | `motion` は**原則省略**（既定 `parallax`。方向はツールが自動割り当て）。意味がある時だけ明示する: 奥行きのある画＝`parallax`（既定）、収束・集中を見せたい＝`parallax_in`、広がり・並び・移動を見せたい＝`parallax_left`/`parallax_right`、平面的な画（壁一面・紙面・グリッド等で視差がほぼ出ない／破綻する）＝`zoom_in`/`zoom_out` |
| `diagram` | 図解。既存の `diagram` スクリプトをそのまま埋める | `narrative` が第一候補（前述）。要素の `at` は**字幕キュー番号**として解釈される（非ビート運用のセグメント番号とは異なる点に注意） |
| `typo` | 決め文の強調カット | 文言を持たない。`at` で対応する `emphasis[].at` を指すだけ（文言は `emphasis[].text` が正） |
| `chapter` | 章カード | シーンの**先頭ビートとしてのみ**許可。`chapter_title` があるとき必須（逆に無ければ先頭を `chapter` にできない） |

- `image`/`diagram`/`chapter` ビートの `slot`（採用素材スロット番号。ファイル名
  `scene_NN_beatM.*`）はシーン内で一意にする。
- キュー番号（字幕の文区切り）はナレーション本文の「。！？」区切りから機械的に決まるため、
  TTS を回さなくても `validate` の採番表で数えられる。執筆時はこの表を見ながら `from` を
  付け直す。

**注目誘導の近似パターン（ポインティングの代替）**

現スキーマには丸囲み・矢印・部分ハイライトのような「ここを見て」を直接指せるフィールドは無い。
代わりに次の組み合わせで代替する。

1. `motion: zoom_in` で対象へ寄る。
2. 同じ画像を別ビートで再提示し、話題の焦点を切り替える（`cut_reason` に切り替えの意図を書く）。
3. `telop` で見てほしい要素を名指しする。

**本物のポインティングが欲しかった箇所は、該当ビートの `cut_reason` 末尾に「(要ポインティング)」
と記録**しておく（将来のツール拡張の判断材料として集める。書くだけでレンダリングには影響しない）。

### 映像リズムの設計（`chapter_title` / `emphasis` / `cutaways` / `diagrams`）

> **ビート運用（`beats`）を使うシーンでは `cutaways` と `diagrams`（複数図解）は使えない**
> （シーン直下に書くとエラー）。全画面インサート・場面転換は `image`/`diagram` ビートを
> 分けて表現する（前述「ビート運用」）。**`chapter_title` と `emphasis` はビート運用でも
> 文言・尺の正本のまま**で、対応する `chapter`/`typo` ビートを添えるだけでよい。以下の
> `chapter_title`・`emphasis` の記述はビート運用でもそのまま有効。`cutaways`/`diagrams` の
> 記述は非ビート運用（v5互換の単一主視覚構成）向け。

シーン境界だけが画面の変わる唯一の瞬間だと、動画全体が一本調子に見える。台本執筆の段階で、
**画面が10〜20秒に1回は変わる**ように設計する（カットアウェイ・複数図解・章カードを使う）。
仕様の詳細は `docs/schema.md`、設計判断の背景は `docs/design-rhythm-and-illustration.md` が正
（いずれも script-to-video 側）。

- **`chapter_title`**: 章（論証の節）の先頭シーンに付ける。3秒の章カードが自動挿入され、
  BGM も自動的にブレイクする。曲を切り替えたい場合は同じシーンに `chapter_bgm` を併記する
  （章替えBGMへ2秒クロスフェード。執筆時は併記を推奨）。**章タイトルは問い文にする**
  （例: `"論証1 ハロー効果"` ではなく `"見た目がいいと性格まで良く見えるのはなぜか"`）。
  章番号は付けない。
- **演出セットは非対称にする**。全シーンに `chapter_title` / `diagram` / `cutaways` を律儀に
  敷き詰めると逆に単調になる。章カードなし・図解なしのシーンを意図的に作り、演出の疎密で
  緩急を付ける。
- **`emphasis`**: 決め文のセグメントに付ける。言い切った直後に、その言葉だけを画面いっぱいに
  出す強調カット（3秒）。**1章に1〜2箇所まで**を目安にする（シーン単位の上限は最大2件。
  付けすぎると強調が効かなくなる）。
- **`cutaways`**: ナレーションが具体物（実験・事例・固有の場面）に触れる箇所に、3〜8秒の
  全画面インサートを挟む。開始/終了セグメント（`from`/`to`）で区間を指定し、素材スロットは
  `slot` で明示する（採用素材は `scene_NN_cutM.*`）。
- **`diagrams`**（複数図解）: 1シーンの中で論点が転換する場合、単一 `diagram` の代わりに
  `from`/`to` で区切って複数の図解を並べる（`diagram` とは排他）。
- **効果音**: 図解の出現・章カード・強調カットには、ツールが既定で効果音を自動付与する。
  執筆時に指定する項目はない。

### 素材候補収集ワークフロー

台本執筆時に、シーンごとの候補素材（画像・動画）と BGM 候補を調査し、YAML に記録する。
最終選択（実際に使う素材の確定）はレンダリング前の候補ギャラリーで人が行うため、ここでの役割は
「候補を集め、ライセンスを確認して記録する」こと。

**説明イラスト（生成AI）は次の順で検討する**（対象: シーン主視覚・カットアウェイ・
背景付き図解の背景のいずれも同じ手順）。

1. `visual_intent` を書く（このシーンを何の絵で見せたいか、日本語1〜2文）。
2. その情景の `gen_prompt` を**必ず起草する**（英語1〜2文。`video.style` と連結して使われる。
   **連結順は `gen_prompt` が先・`video.style` が後**——様式強制句を先頭に置くと主題への追従が
   弱くなることが実測されたため R19.6 で反転した。様式語を `gen_prompt` 側に重複して書く必要は
   ない）。
   **型は2つ使い分ける**。1つは**情景型**——「人物主語＋小道具＋一瞬のポーズ」
   （例: "a researcher pausing mid-sentence while pointing at two photographs on a table"）。
   抽象語だけの表現は避け、「誰が・何をしている場面か」を具体的に描写する。もう1つは
   **擬物化静物型**——抽象概念を、その構造を体現する「物」のメタファーで、シックな照明の静物として
   描く型（例: "an old brass balance scale on a dark desk, one pan slightly lower, warm lamplight"
   ＝比較・不均衡）。論証の主張そのものを画で支えたい場面（従来は無地図解に頼っていた箇所の一部）
   で使う。判断基準は、場面・人物の空気感を見せたい→情景型、概念の構造そのものを見せたい→
   擬物化静物型。

   擬物化静物型の汎用メタファー辞書（型の例。これ以外のメタファーを考えても構わない）:

   | 主張の型 | メタファー例 |
   |---|---|
   | 比較・不均衡 | 天秤／高さの違う積み木 |
   | 蓄積・増加 | 積み上がる硬貨・書類の山 |
   | 比率・多数 | 並んだ駒・椅子の群れ |
   | 選別・淘汰 | ふるい／開く扉と閉じる扉 |
   | 連鎖・波及 | ドミノ／水面の波紋 |
   | 対立・拮抗 | 綱引きのロープ／向き合う駒 |
   | 時間・経過 | 砂時計／伸びる影 |
   | 隠れた構造 | 氷山／水面下 |

   偽文字対策（下記）・before/after 不可（下記）・顔クローズアップ回避・検品3観点・再生成2回まで
   （後述「生成した説明イラストは採用前に検品する」）は型を問わず共通のルールとして擬物化静物型
   にもそのまま適用する。

   `video.style` の推奨文言（全編共通で1箇所、シネマティック様式）は
   `docs/schema.md` の `video.style` 節が正。用途別（主視覚／カットアウェイ／図解背景）の
   書き分けは script-to-video の `docs/design-rhythm-and-illustration.md` §6.3 を参照。
   **文字物（書類・書籍・看板・画面など）を描く場合は、読めない／白紙であることを明示する句を
   必ず添える**（偽文字対策。生成AIは文字らしき模様を描くが、読める文字列にはならないため）。
   例: 紙・書類は `blank paper with no writing` / `the form blurred and unreadable`、本・学術誌は
   `pages out of focus with no readable print`、看板・画面は `no text, no lettering`。
   この句が欠けていると疑われる `gen_prompt` は `validate` が警告する。詳細は `docs/schema.md`
   の `gen_prompt` 節・`docs/design-beat-rendering.md` §9.4 を参照。
   **before/after・二者比較・時系列を含む情景は生成AIに投げない**（1枚の静止画に複数時点・
   複数状態は描き分けられないため）。`diagram`（`comparison` 型。ビート運用では `narrative`
   型で言い換えられないか先に検討する）に置き換えるか、単一被写体の一場面に還元してから
   `gen_prompt` を書く。**ビート運用では、時系列（before/after）は `image` ビートを2つに分けて
   時間軸で見せる方法もある**（同一シーンに複数の `image` ビートを置けるため）。還元先は
   顔のクローズアップではなく小道具・UI操作の場面を選ぶ（顔のクローズアップは写実調へ
   様式崩れしやすいことを型検証で確認済み。`references/20260819-genprompt-type-validation.md`
   参照）。
3. ストック素材で代替できるか判断する。一般名詞的な情景（街の雑踏、スマートフォン操作、
   統計オフィスなど）や、実写であることに意味がある画（報道写真的なもの）は
   **代替できるならストックを優先**する。代替できなければ `gen_prompt` を第一候補のまま残す
   （候補ギャラリーでの明示操作・課金承認制は変わらない）。

**数の見せ方の使い分け**

同じ「数」を見せたい場面でも、正確さが要るか印象でよいかで手段を変える。

| 見せたいもの | 手段 | 注意 |
|---|---|---|
| 正確な数値（推移・大小の比較） | `diagram` の `chart` | 裏取り済み数値のみ・動画1本1箇所まで（既存規則。上記「型の選び方」参照） |
| 規模感・比率の「印象」 | 擬物化静物型の `gen_prompt` | 生成AIは個数を正確に描き分けられないため、数字の正確さは絵に負わせない。ナレーションと `chart` が数字を担い、絵は「一方が多い」の印象だけを担う（例:「105対100」を駒の数で正確に見せる、のような使い方はしない） |
| 選別・一部が選ばれる | `narrative` の `row` | 既存（上記「`narrative` の3レイアウト」参照） |

**生成した説明イラストは採用前に検品する**（詳細は script-to-video の
`docs/design-chic-tone.md` §8 が正）。

1. `video.style` からの逸脱がないか（特に写実化。全編の画風統一が前提）。
2. ナレーション・`visual_intent` の内容と一致しているか（人数・場所・時代・持ち物のずれに注意）。
3. 人物の並置などによる意図しない含意（比較・優劣・因果の読み違え）がないか。

同一のビジュアル意図での再生成は**最大2回まで**。3回目に入りそうなら、生成を続けずに
`diagram`（`comparison` 型）へ切り替える。

**画像生成の運用（自動が主・手動生成は例外）**

画像生成の主力は従来どおり自動（Stability、`gen_prompt` 起草→パイプライン生成）。**構図が複雑で
自動生成が2回失敗した場合や、サムネ・オープニング級の「ここぞの1〜3枚」に限り**、ChatGPT Plus
での手動生成を許可する。

1. YAML には通常どおり `visual_intent` と `gen_prompt` を書く（再現性の記録として残す）。
2. ChatGPT には「`gen_prompt` ＋ `video.style` の様式文」を渡して生成する。
3. できた画像は素材ディレクトリに該当スロット名（`scene_NN_beatM.png` など）で配置し、候補として
   採用する。
4. 検品3観点＋偽文字チェックは自動生成と同様に適用する。
5. 手動生成であることを、YAML の該当ビート近くに **YAML コメント**で明記する（コメントはスキーマ
   の対象外で `extra="forbid"` に抵触しない）。

画風統一の注意: `video.style` の文言を必ずプロンプトに含める。全編の中で1枚だけ画風が浮いたら
不採用にする。

**冒頭シーンの実写素材は静止画を選ぶ**（動画素材には Ken Burns 効果が掛からないため）。

**実物引用ビート（実写・実物画像で説明する）**

ナレーションが具体物（論文の図表・地図・統計グラフ・歴史写真等）に触れる箇所は、生成AIの雰囲気
画像ではなく**実物の画像を `image` ビートの `assets` として出す**ことを検討する（説得力が最も
強い選択肢として、`gen_prompt` より優先して検討してよい）。

- ライセンスは限定する。パブリックドメイン（`license: pd`）／CC BY 系（`license: cc-by`。
  `source_url` と `attribution` が既存規則どおり必須）／**日本の政府統計・官公庁サイト**
  （政府標準利用規約は CC BY 互換のため `license: cc-by` として記録し、`attribution` に
  出典を書く）の3種に限る。汎用画像検索由来（`license: unknown`）は引用用途では使わない
  （権利未確認の画像で「これが実物です」と示すのは事実性のリスクが高いため）。
- 出典は必ず `telop` で表示する（既存の出典表示規則と同じ）。`source_url`・`attribution` の
  記録は既存の `assets` 規則どおり。
- 論文図表は原著の Figure をそのまま使える場合のみ使う（改変しない）。使えない場合は数値を
  `chart` に起こす（既存路線。上記「数の見せ方の使い分け」参照）。

ストックの候補を集める場合は、次の手順でライセンスを確認して記録する。

1. シーンごとに素材候補を **2〜4件**、ライセンス情報とともに YAML の `assets` に記録する。
2. ストック素材は **Pixabay / Pexels**（商用利用可・クレジット表記不要）に限定して探す
   （YAML では `license: commercial-ok`）。
3. Wikimedia Commons の候補は PD か CC の別を確認する。CC の場合は出典表記文字列を
   `attribution` に記録する（`license: cc-by` の場合、`source_url` と `attribution` が必須）。
4. ライセンス不明（汎用画像検索由来）の候補は `license: unknown` とし、`source_url` を
   必ず記録する（権利要確認マーク）。
5. 存命人物が写る候補には、肖像権・パブリシティ権の注意を `note` に付記する。
6. BGM は商用利用可のフリー音源（例: DOVA-SYNDROME）から候補曲を検索し、ライセンス確認結果と
   URL を提示する。ダウンロード・配置はユーザーが行うため、YAML の `video.bgm` には
   （配置予定の）ファイルパスのみ記録する。BGM が確定したら、クレジット表記文字列を
   `video.bgm_credit` に記録する（`description.txt` に自動転記される。省略時はファイル名
   ベースのフォールバック表記になる）。
- `search_query`（素材検索クエリ）は **英語** で書く（Pixabay / Pexels / Commons の検索に
  渡すため）。

> **凍結中の旧運用（HeyGen 前提）**: 運用は AI Studio（Web手動・Creator プラン）。API は使わず、
> 台本（Markdown）をマスターとし、HeyGen に流し込む **シーンテーブル CSV**
> （`scripts/{台本と同名}.heygen.csv`。列: `scene, 読み上げテキスト, テロップ, ビジュアル素材,
> 発音ポーズメモ`。雛形 `templates/heygen-scenes.csv`）を併せて出力していた。
> 過去台本（HeyGen 期のもの）を参照する際の背景情報として残す。

## ディレクトリ構成

- `scripts/` — 台本マスター（`YYYYMMDD-{テーマ}.md`）とシーン YAML（`{同名}.yaml`）。
  凍結中の HeyGen 期の台本には、代わりに取り込み用 CSV（`{同名}.heygen.csv`）が付随する。
- `references/` — 調査メモ・出典リスト・裏取り結果。
- `templates/` — 台本の雛形（`script-template.md`）とシーン YAML の雛形
  （`scene-yaml-template.yaml`）。凍結中の HeyGen 用 CSV 雛形（`heygen-scenes.csv`）も
  過去台本参照用に残置する。

## イシュー駆動ワークフロー（GitHub Issues 起点）

修正も新規制作も **GitHub Issue を起点** にし、修正の経過を Issue とコミットに残す。
リポジトリ: `shuyahara/draft-explanation-video`。操作は `gh` CLI（認証済み）。

1. **起点**: Issue を立てる。`/issue {内容}` または `gh issue create`。
   - 修正 → `[修正]` テンプレ（対象台本・シーン／問題／期待結果／出典要否）。
   - 新規 → `[新規]` テンプレ（テーマ／想定尺／結論方向／引用候補）。
2. **着手**: 作業前に方針を Issue にコメント（`gh issue comment {N} -b "..."`）。
3. **作業**: `/draft` 等で台本を編集。**裏取り結果・判断の根拠・却下した案** は逐次 Issue コメントに残す
   （ここが「修正の経過」の本体）。
4. **コミット**: 1論点1コミット。メッセージ本文に Issue 番号を入れて紐付ける（`#N`）。
   何を・なぜ変えたかを書く。
5. **完了**: 最後の関連コミットに `Closes #N` を入れて自動クローズ、または
   `gh issue close {N}` 時に最終要約コメントを残す。

> Issue を見れば「指摘 → 調査 → 裏取り → 判断 → 変更」の経過が時系列で追え、
> 各変更は `#N` でコミットに辿れる状態を保つ。

## ワークフロー（コマンド）

- `/issue {内容}` … 内容から修正/新規を判断し、テンプレに沿った Issue を作成。
- `/draft {テーマ}` … 台本ドラフトを生成（出典の裏取り＋素材候補収集＋シーン YAML 出力まで）。Issue 起点なら `#N` を渡す。
- モデルは **opus**（高度な推論）を既定とする（`.claude/settings.json`）。
