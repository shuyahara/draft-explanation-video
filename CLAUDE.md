# draft-explanation-video

機械音声（TTS）解説動画の **台本** を作成するプロジェクト。

> **HeyGen 運用は凍結（2026-08-14）**。新規台本は自作レンダリングツール
> [script-to-video](https://github.com/shuyahara/script-to-video) 向けに執筆する。
> 背景・過去台本参照用の詳細は docs/legacy-heygen.md。

## このプロジェクトでの Claude の役割

ユーザーが与えたテーマについて、**論拠を積み上げて結論を導く** 解説動画の台本を執筆する。
単なる情報の列挙ではなく、「問い → 根拠 → 反証の検討 → 結論」という論証構造を持たせる。

**台本のナレーション本文は、オーケストレーター（Fable）本人が執筆・改稿する**（2026-08-24
ユーザー指示。グローバル設定の「執筆はワーカー委譲」より本ルールが優先）。調査・裏取り・
シーン YAML の組み立て・素材候補収集・レンダリングなどの周辺作業はワーカー委譲のままでよいが、
読み上げ文の創作・推敲はワーカーに委譲しない。ワーカーが YAML 化などで文言変更が必要になった
場合は、変更案をオーケストレーターに差し戻して判断を仰ぐ。

## 動画・台本の前提

- **尺**: 1本あたり 10〜30 分を想定。
- **読み上げ速度の目安**: 日本語 TTS はおよそ **300〜360 文字/分**。
  - 10 分 ≒ ナレーション 3,000〜3,600 文字
  - 20 分 ≒ ナレーション 6,000〜7,200 文字
  - 30 分 ≒ ナレーション 9,000〜10,800 文字
  - 台本作成時は冒頭に想定尺と概算文字数を明記し、本文の文字数を合わせる。
- **音声**: 機械音声で読み上げる。後述の「機械音声向けの書き方」を必ず守る。

## 台本フォーマット（ナレーション＋画面指示）

シーン単位で「読み上げ文」と「画面指示」を併記する。`templates/script-template.md` を雛形とする。
**1シーン＝シーン YAML の `scenes[]` の1件**に対応する（音声・字幕・章の単位）。ただし
**v6 以降、シーン内の画面は `beats[]`（ビート）単位で複数回切り替わるのが標準**であり、
「1シーン＝1枚の主視覚」という旧来の粒度ではない（詳細は後述「ビート運用」）。
読み上げ文は目安として **約1,000文字以内**（シーンの粒度として引き続き有効なため維持する。
長くなる場合は論証の区切りでシーンを分ける。この目安の由来は docs/legacy-heygen.md）。
雛形は「ナレーション」「画面（テロップ・ビジュアル・出典表示・発音ポーズメモ）」の2部構成。

全体構成の基本形:
1. **フック（つかみ）** — 視聴者の関心を引く問い・意外な事実・結論の予告。冒頭15秒以内に
   この動画が答える問いを言語化する。
2. **問題提起** — 何を、なぜ問うのか。
3. **論証本体** — 根拠を1つずつ提示。各根拠に出典をつける。反証・反論も検討する。
4. **結論** — 積み上げた論拠から導かれる結び。冒頭で言語化した問いに明示的に答えて閉じる。
5. **まとめ／次への誘導** — 要点の再確認。

**章の設計**（論証型・探索型いずれの構成でも共通）:
- 各章末は次章へつながる問いで終える（「では〜はどうなのか」等）。視聴者の関心を章をまたいで
  引っ張る。
- 同一構文の反復を3回以上作らない。特に研究紹介（「〜年、〜らは〜と報告した」型）は結論先出し・
  当事者視点・数字先出しなど語り口を混ぜ、同じ構文が3件連続しないようにする。

**代替の全体構成: 探索型**（v6 で試用。尺は8〜10分目安。論証型の代わりに発見の過程を
追体験させたい場合に選ぶ）
1. 具体的な一人の場面で開く（フック）。
2. その場面を問いへ変換する。
3. 問いを調べていく過程として研究を提示する（結論を先に言わない）。
4. 中盤で前提を反転させる（「実は〜だった」）。
5. 冒頭の場面へ戻り、問いに答える。

## 引用・ファクトの扱い（出典必須・検証あり）

解説の説得力は引用の正確さに依存する。**捏造は厳禁**。

- 論文・実験・統計・著名人の言葉・名言を引用する場合、必ず **出典**（著者／タイトル／発表年／可能なら URL）を添える。
- 引用する事実は **WebSearch / WebFetch で裏取り** する。記憶だけに頼らない。
- 裏取りできなかった引用は **台本に載せない**、もしくは「要確認」と明示してユーザーに判断を委ねる。
- 名言は「誰の言葉か」を確認する（誤帰属が多い領域なので特に注意）。
- **研究・分析・調査を紹介するときは、実施機関と実施者をなるべくナレーション本文で明記する**
  （「どこの大学の・誰の研究か」。2026-08-24 ユーザー指示）。特に大学名自体に権威がある場合
  （ハーバード、イェール、東大など）は必須。ただし帰属は一次資料で確認できたものだけを書き、
  確認が不十分な場合は「〜などの研究チーム」のように含みを持たせる（不正確な権威付けは逆に
  信頼を損なう）。語り口は反復回避規約に従い主体先出し・数字先出し・結論先出しを混ぜてよいが、
  帰属自体は各研究につき必ず一度は述べる。
- 出典は台本内に脚注的にまとめ、かつ画面指示の「出典表示」にも反映する。

## 機械音声向けの書き方

話者は **Google Cloud TTS Chirp3 HD の Enceladus** を標準とする（レンダは
`--tts-backend gcp --tts-voice ja-JP-Chirp3-HD-Enceladus`）。ナレーションはシーン YAML の
`narration` セグメントとして記録する。詳細（ポーズ設計の目安・実測値・根拠文献・クレジット）は
docs/narration-style.md。

- 読み上げテキストはプレーンに（記号・マークダウン・ルビ括弧を入れない）。
- 読み修正は YAML の `readings` で指定し、台本の「発音・ポーズメモ」にも書く。
- 間は `pause_after`（秒）で指定する。3段階以上を使い分け、文境界で分割するときは省略せず最低 0.2 秒を明示する。
- 二人称の問いかけを各章1回以上入れる。
- 1文を長くしすぎない。同音異義語を避ける。視覚依存の表現（「左の図」等）はナレーション単体で意味が通るよう補う。箇条書き・記号は telop 側へ。

## script-to-video 向け出力

台本（Markdown）を人間用マスターとし、レンダリング指示書として **シーン YAML** を併せて出力する。
YAML はレンダリングツール [script-to-video](https://github.com/shuyahara/script-to-video) の
入力であり、ツールはこの YAML だけを読んで実行時に決定的に動画を生成する（LLM は呼ばない。
検索語・読み・プロンプトはすべて YAML に確定させておく）。

- **YAML の仕様は script-to-video 側が正**。本リポジトリでは仕様を複製しない。
  - ローカル: `C:\Users\shuya\Projects\script-to-video\docs\schema.md`
  - GitHub: `shuyahara/script-to-video` リポジトリの `docs/schema.md`
  - サンプル: `script-to-video/examples/sns-lookism-sample.yaml`
  - ビート運用（`beats[]`。v6 以降の標準）の設計判断は同リポジトリの
    `docs/design-beat-rendering.md` が正。詳細は後述「ビート運用」。
- **保存先**: `scripts/{YYYYMMDD-テーマ}/{台本と同名}.yaml`。雛形は `templates/scene-yaml-template.yaml`。
- 未知のフィールドはツール側でエラーになる（`extra="forbid"`）。仕様にない項目は書かない。
  仕様に不足を感じた場合は独自拡張せず、まずユーザーに相談する。
- 完成した YAML は script-to-video の `validate` サブコマンドで検証する。
  ```
  cd C:\Users\shuya\Projects\script-to-video
  .venv\Scripts\python.exe -m script_to_video validate <yamlパス>
  ```
- 出典は音声で読み上げず、原則 `telop`（画面テロップ）で表示する（読み上げたい場合はナレーションに含める）。

### 図解スクリプト（`diagram`）

抽象概念・因果・比較・データは図解（`diagram`）で表現できる。書くのは型・要素・ラベル・出現位置（`at`）だけで、座標・色・アニメーションは書かない。仕様は script-to-video の `docs/schema.md`、執筆判断の詳細（画面構成・型の選び方・narrative の4レイアウト・手順・チェックリスト）は docs/diagram-guide.md。

**最上位規則**（詳細と条件は docs/diagram-guide.md）:
- ビジュアル意図（`visual_intent`）を最初に決め、図解・検索語・生成プロンプトはそこから導く。
- **採用テスト**: 「見て初めて増える情報」（裏取り済み数値2点以上／構造配置が知見そのもの／比率・選別の可視化）がある図解だけ書く。ラベルがナレーションの写しの図解は書かない。担う構造情報を cut_reason に明記する。
- ビート運用では `narrative`（radiate / converge / row / chain）が第一候補。ピル型（buildup / flow / comparison）は chart を除き非推奨。
- 同一の型・layout は動画1本につき2回まで、隣接する図解ビートに同じ layout を使わない。図解総数は10分あたり5〜6箇所。
- `chart` は裏取り済み数値のみ・動画1本に1箇所まで。単発の数値は図解ではなくビートの telop に載せる。
- 図解ビートは背景付きが既定（無地は「引き締め」の例外。無地を2連続させない。chart は無地可）。
- 図解ラベル・telop・emphasis の同一文言を約10秒以内に2度出さない。

### ビート運用（`beats[]`。v6 以降の標準）

v6 以降、シーンの映像は `beats[]`（image / diagram / typo / chapter）の列で設計する。仕様は `docs/schema.md`、設計背景は `docs/design-beat-rendering.md`（いずれも script-to-video）、執筆判断の詳細は docs/beat-guide.md。v5 互換の非ビート運用（cutaways / diagrams / emphasis）は docs/legacy-v5-rhythm.md。

**守る規則**（詳細は docs/beat-guide.md）:
- ビート境界は「意味の節目」（話題・主張・場面の変わり目）に置く。一枚絵の保持は 8〜15 秒目安（20秒超は validate が警告）。長い1文は執筆段階で2文に分けてキュー境界を作る。
- 大きな転換はセグメント境界（`pause_after` のある位置）に合わせる。
- 図解の決め要素の `after` は句の終端語に置かない（終端の1〜2秒手前の語に同期させる）。
- 各ビートに `cut_reason`（「【何の場面か】なぜここで切るか」）を必ず書く。
- 決め文の文字カット（emphasis＋typo）は新規台本では使わない。決め文は画面を保持したまま `pause_after` 2.0〜2.5 秒の間を取る。
- 動画の最後は象徴的な一枚絵で終え、アウトロは `--outro-seconds 8` を標準とする。
- 列挙・収束の図解だけでシーンを長く持たせない（列挙は image ビート、図解は収束部だけ）。
- 章カード（`chapter_title`）: 章タイトルは問い文にし、章番号は付けない。演出セットは非対称にする（章カードなし・図解なしのシーンを意図的に作る）。

### 素材候補収集ワークフロー

シーンごとの候補素材と BGM 候補を調査して YAML に記録する（最終選択は候補ギャラリーで人が行う）。手順・メタファー辞書・ライセンス規則の詳細は docs/asset-workflow.md。

**守る規則**（詳細は docs/asset-workflow.md）:
- 順序は `visual_intent` を書く → `gen_prompt` を必ず起草する（英語1〜2文。情景型／擬物化静物型）→ ストックで代替できるか判断する。一般名詞的な情景や実写に意味がある画はストック優先。
- **直接描写を既定にする**: 対立・議論・関係そのものが主題の場面は、静物メタファーに逃げずに主題を直接描く情景をまず検討する。
- 文字物（書類・書籍・看板・画面）を描くときは「読めない／白紙」の句を必ず添える（偽文字対策）。before/after・二者比較・時系列は1枚に描かせない（image ビートを分けるか図解へ）。顔のクローズアップは避ける。
- 生成イラストは採用前に3観点（画風統一・内容一致・意図しない含意）で検品する。同一意図の再生成は2回まで、3回目は別モチーフで再起草する。
- ストック素材は Pixabay / Pexels に限定（`license: commercial-ok`）。`search_query` は英語。実物引用は PD / CC BY / 政府統計に限る。BGM は商用可フリー音源から候補を出し、確定後 `video.bgm_credit` に記録する。
- 冒頭シーンの実写素材は静止画を選ぶ。

## ディレクトリ構成

- `scripts/` — 動画1本につき1フォルダ（`scripts/YYYYMMDD-{テーマ}/`）。中に台本マスター（`YYYYMMDD-{テーマ}.md`）とシーン YAML（`{同名}.yaml`）を置く。YAML の `video.bgm` は `../bgm/{曲名}.mp3`（`scripts/bgm/` は全動画共有）。
- `references/` — 調査メモ・出典リスト・裏取り結果。
- `templates/` — 台本の雛形（`script-template.md`）とシーン YAML の雛形（`scene-yaml-template.yaml`）。
- `docs/` — 参照資料（詳細・表・手順・判断ログ）。一覧は末尾「参照ドキュメント」。

凍結中の HeyGen 期の台本・雛形の背景は docs/legacy-heygen.md 参照。

## イシュー駆動ワークフロー（GitHub Issues 起点）

修正も新規制作も **GitHub Issue を起点** にし、修正の経過を Issue とコミットに残す。
リポジトリ: `shuyahara/draft-explanation-video`。操作は `gh` CLI（認証済み）。

1. **起点**: Issue を立てる。`/issue {内容}` または `gh issue create`。
   - 修正 → `[修正]` テンプレ（対象台本・シーン／問題／期待結果／出典要否）。
   - 新規 → `[新規]` テンプレ（テーマ／想定尺／結論方向／引用候補）。
2. **着手**: 作業前に方針を Issue にコメント（`gh issue comment {N} -b "..."`）。
3. **作業**: `/draft` 等で台本を編集。**裏取り結果・判断の根拠・却下した案** は逐次 Issue コメントに残す
   （ここが「修正の経過」の本体）。
4. **コミット**: 1論点1コミット。メッセージ本文に Issue 番号を入れて紐付ける（`#N`）。
   何を・なぜ変えたかを書く。
5. **完了**: 最後の関連コミットに `Closes #N` を入れて自動クローズ、または
   `gh issue close {N}` 時に最終要約コメントを残す。

> Issue を見れば「指摘 → 調査 → 裏取り → 判断 → 変更」の経過が時系列で追え、
> 各変更は `#N` でコミットに辿れる状態を保つ。

## ワークフロー（コマンド）

- `/issue {内容}` … 内容から修正/新規を判断し、テンプレに沿った Issue を作成。
- `/draft {テーマ}` … 台本ドラフトを生成（出典の裏取り＋素材候補収集＋シーン YAML 出力まで）。Issue 起点なら `#N` を渡す。
- モデルは **opus**（高度な推論）を既定とする（`.claude/settings.json`）。

## 参照ドキュメント

`docs/` 配下は本書の規則の詳細・表・手順・判断ログ（正本は本書 CLAUDE.md）。

- `docs/legacy-heygen.md` — 凍結中の HeyGen 運用の背景・過去台本参照用の詳細。
- `docs/diagram-guide.md` — 図解スクリプト（`diagram`）の画面構成・型の選び方・narrative の4レイアウト・書き方の手順・チェックリスト。
- `docs/beat-guide.md` — ビート運用（`beats[]`）の境界設計・決め文の規則・ビート種別・ポインティング代替。
- `docs/legacy-v5-rhythm.md` — 非ビート運用（v5互換）の映像リズム設計（`chapter_title` / `emphasis` / `cutaways` / `diagrams`）。
- `docs/asset-workflow.md` — 素材候補収集ワークフロー（説明イラストの検討手順・メタファー辞書・検品・ストック/BGM のライセンス規則）。
- `docs/narration-style.md` — 機械音声向けの書き方の詳細（ポーズ設計の目安・実測値・根拠文献・クレジット）。
- `docs/decisions.md` — 日付・R番号・実測値・試写FB に基づく判断の索引。
